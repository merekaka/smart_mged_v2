from django.apps import AppConfig


class IntentEngineConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.follow_up.intent_engine'
    verbose_name = '意图理解引擎'
