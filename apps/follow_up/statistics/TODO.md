# 统计分析功能 TODO 清单

## 已完成功能

### 1. 饼图（proportion）— 类别占比统计
- [x] 支持指定属性统计不同值的占比
- [x] 空值（None/空字符串）单独统计为"未填写"类别
- [x] 小占比（<2%）自动合并为"其他"
- [x] 数值型字段自动检测并给出警告提示
- [x] 前端 ECharts 饼图渲染

**支持的提问示例**：
- "画个饼图看看各材料的占比"
- "统计一下显微组织的分布"
- "看看都是什么标准"

**期望输出**：环形饼图，含图例、tooltip、标签

---

### 2. 柱状图（distribution）— 数值区间分布
- [x] 数值字段自动分箱（Sturges' formula）
- [x] 支持自定义分箱数量
- [x] 附带统计量（最小值、最大值、平均值、中位数）
- [x] 前端 ECharts 柱状图渲染

**支持的提问示例**：
- "抗拉强度的分布情况"
- "温度主要分布在哪些区间"

**期望输出**：柱状图 + 统计指标表格

---

### 3. 排序表格（sort）
- [x] 支持升序/降序排列
- [x] 支持限制返回前 N 条
- [x] 数值型自动提取数字比较
- [x] 前端表格渲染

**支持的提问示例**：
- "按弹性模量从高到低排序"
- "按抗拉强度排一下"

**期望输出**：排名表格（data_id、材料名称、值）

---

### 4. 描述统计（summary）
- [x] 计算样本数、均值、标准差、最小值、最大值、中位数
- [x] 支持单字段或多字段统计
- [x] 前端表格渲染

**支持的提问示例**：
- "描述一下密度的统计特征"
- "统计一下抗拉强度和硬度"

**期望输出**：统计指标表格

---

### 5. 折线图（trend）— 双字段趋势分析
- [x] 后端代码已实现（`ops/trend.py`、`charts/line.py`）
- [x] 支持 x_field + y_field 双字段分析
- [x] Prompt 已添加 `trend` 说明和示例（`parser.py`）
- [x] 分类器已添加 `trend` 子类型校验（`classifier.py`）
- [x] 前端 ECharts 折线图渲染已实现（`intent_chat.js`）

**支持的提问示例**：
- "温度对抗拉强度的影响"
- "看看温度对弹性模量的影响趋势"

**期望输出**：折线图（带平滑曲线、面积填充、数据点标记）

---

## 待修复问题

> 当前无待修复问题。所有已识别问题均已解决。

---

## 代码结构优化建议

### 已完成的重构
- [x] 将 `operations.py` 拆分为 `ops/` 目录下的独立模块
- [x] 将 `chart_builder.py` 拆分为 `charts/` 目录下的独立模块
- [x] 提取公共工具函数到 `ops/base.py`

### 建议的进一步优化
- [ ] 为每个统计操作添加单元测试
- [ ] 添加操作执行耗时统计（性能监控）
- [ ] 考虑将 `formatter.py` 也按操作类型拆分

---

## 功能扩展建议

### 短期（高优先级）
1. 为饼图添加点击交互（点击扇区可筛选数据）
2. 为柱状图添加数据缩放（dataZoom）支持大数据量

### 中期（中优先级）
3. 支持多字段对比柱状图（如对比不同材料的抗拉强度）
4. 支持散点图（分析两个数值属性的相关性）
5. 支持箱线图（展示数据分布的统计特征）

### 长期（低优先级）
6. 支持用户自定义图表类型（通过自然语言指定）
7. 支持图表导出（PNG/SVG/CSV）
8. 支持图表联动（多个图表之间的交互筛选）

---

## 测试用例

### 饼图测试
```
输入："统计一下显微组织的占比"
期望：
  - analysis_op: "proportion"
  - field: "microstructure"
  - 饼图包含：α型钛合金、β型钛合金、α+β型钛合金、未填写 等类别
```

### 柱状图测试
```
输入："抗拉强度的分布情况"
期望：
  - analysis_op: "distribution"
  - field: "tensile_strength"
  - 柱状图展示区间分布 + 统计指标
```

### 折线图测试
```
输入："温度对抗拉强度的影响"
期望：
  - analysis_op: "trend"
  - field: "tensile_strength"
  - config.x_field: "temperature"
  - 折线图展示趋势变化
```

---

*最后更新：2026-05-23*
